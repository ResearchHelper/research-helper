/**
 * These are provided by ProjectBrowser.vue and injected into its components
 */
// project
export const KEY_metaDialog = Symbol(); // update project
export const KEY_deleteDialog = Symbol(); // delete project
export const KEY_attachFile = Symbol(); // attach pdf to project
export const KEY_renameFromMeta = Symbol(); // rename pdf from project meta

// note
export const KEY_addNote = Symbol();
export const KEY_renameNote = Symbol();
export const KEY_deleteNote = Symbol();
